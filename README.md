# PostClip 1.1.2

Xの投稿URLから、スマホ幅の見やすいPNG画像を作成するWindows用アプリです。
無料・機能制限なし・透かしなし。撮影用サーバーや有料APIは使いません。

## 起動する

1. `postclip-win-x64-v1.1.2.zip` を右クリックし、「すべて展開」を選びます。
2. 展開した `PostClip` フォルダーの `PostClip.exe` を開きます。
3. 投稿のURLを貼り付け、「画像を作成」を押します。
4. プレビューを確認し、「PNGを保存」を押して保存先を選びます。

`PostClip.exe` だけを移動せず、同じフォルダーのDLL・resources・localesなども一緒に置いてください。
Python・Node.js・ブラウザー拡張機能のインストールは不要です。

## 対象環境

- Windows 10 / 11、64bit（x64）
- インターネット接続
- 展開後のアプリに約400MBの空き容量。画像作成時は追加のメモリーを使います。
- macOS、Linux、Windows ARM64のネイティブ版はこの配布ZIPには含みません。

配布用EXEにはコード署名がありません。Windowsの確認画面が出る場合は、配布元を確認してください。

## 幅と画質

| 設定 | 内容 |
| --- | --- |
| 表示幅 | 320〜550px。375 / 400 / 430pxのボタン、または数値入力 |
| 解像度 | 1倍 / 2倍 / 3倍 |
| 標準 | 表示幅400px × 3倍 = 保存画像の横幅1,200px |
| 縦の長さ | 表示された対象投稿の下端まで自動調整 |
| コミュニティノート | 初期設定でオン。投稿画面に表示されたノートを含める |
| 含める投稿 | この投稿のみ / 直前の返信先 / 一番上の親まで |
| テーマ | ライト / ダーク。投稿画面モードではX側の色を使用 |
| 外側の余白 | なし / 16px / 24px。保存画像の幅にも加算 |

文字は指定した解像度でブラウザーから描画します。元の投稿画像やプロフィール画像自体が低解像度の場合、その画像の細部までは増えません。

## 2つの表示方法

### かんたん — 公式埋め込み

「コミュニティノートを含める」をオフにすると選べます。X公式の埋め込み表示を読み込み、そのDOMとCSSを高解像度で撮影します。公開投稿向けで、APIキーは不要です。Xの通常画面とは、操作ボタン・日時・引用の表示方法などが異なる場合があります。

### 投稿画面 — Xの表示を撮影

標準設定ではこちらを使います。Xの投稿ページを開き、対象の投稿部分を撮影します。「Xを開く・ログイン」で必要なログインを行い、この画面へ戻ってから作成します。通常のブラウザーとは別のログイン情報を使います。Xの画面構造やアクセス制限に影響されるため、埋め込みモードで取得できる投稿でも、このモードでは取得できないことがあります。

ログインにはXの通常のログイン方法を使用してください。Google / Appleなど外部サイトへのログイン遷移は対応していません。

## コミュニティノートを含める

「コミュニティノートを含める」は初期設定でオンです。表示方法の選択にかかわらずXの投稿画面を使い、表示されたノートも保存します。ノートが表示されていない投稿も、設定を変えずに保存できます。必要に応じて「表示方法・余白を調整」内の「Xを開く・ログイン」でログインしてください。

旧版から更新した場合も、最初の起動時にノート設定をオンへ移行します。表示幅・解像度・含める投稿などは引き継ぎます。その後は、画像作成時に選んだオン・オフを保存します。

公式埋め込みでは、通常の投稿画面にあるノートが省略される場合があります。そのため、この設定がオンの間は投稿画面から撮影します。ノートの表示がない場合はその旨を完成画面に表示します。読み込み途中のノートを検出した場合は本文を待ち、確認できなければ画像を作らず案内します。

親まで含める場合も、各投稿画面に表示されたノートを一緒に保存します。表示内容・言語・公開状態は撮影時のXの画面に従います。非表示のノートの有無を確定したり、取得できない内容を補完したりする機能ではありません。

## 撮影する範囲

- 「この投稿のみ」「直前の返信先も含める」「一番上の親まで含める」を選べます。
- 「一番上の親まで含める」は、返信先のリンクをたどり、最初の親から指定した投稿までを古い順に1枚に収めます。公開投稿向けで、指定した投稿を含め最大20件です。別枝の返信や引用先はたどりません。
- 途中に削除・非公開・埋め込み非対応の投稿がある場合、親までの取得を中止して理由を表示します。一部だけを完成扱いにはしません。投稿画面モードでも、親子関係の確認には公開埋め込みを使います。
- 返信一覧やタイムライン全体を無限スクロールして収集する機能ではありません。
- 長文や引用がX側で省略されている場合、表示された範囲までの画像になります。省略の検出通知だけに頼らず、保存前にプレビューを確認してください。
- 動画・GIFは静止画として保存します。動画ファイルのダウンロード機能ではありません。
- 高さ30,000pxまたは約3,000万画素を超える場合は、画像の一部を切り落とさずエラーにします。解像度を下げて再試行してください。

## 便利な操作

- URL欄の「貼り付け」：クリップボードの文字列を入力
- Ctrl + Enter：画像を作成
- 「コピー」：完成画像をクリップボードへコピー
- 「実寸で見る」：保存ピクセル数で表示。大きい画像はスクロールできます。
- 「作成をキャンセル」：読込と撮影を中止
- 右上の「？」：使い方とログイン情報の削除

## データと通信

PostClip独自のサーバー、利用状況解析、広告配信はありません。投稿と画像を取得するため、XおよびXの配信元（x.com / twitter.com / twimg.comの各ドメインとサブドメイン）へ通信します。それ以外の外部通信は遮断します。Xへの通信にはX側の利用条件・プライバシーポリシーが適用されます。

完成PNGは保存操作までメモリーに保持されます。アプリ独自の投稿URL履歴は保存しません。設定と、投稿画面モードのログイン情報・ブラウザーキャッシュは、Windowsのアプリデータフォルダー（通常は `%APPDATA%\PostClip`）に保存されます。右上「？」からアプリ内のXログイン情報を削除できます。

## うまくいかないとき

| 状況 | 対処 |
| --- | --- |
| 投稿を取得できない | URL・ネット接続・X側の公開状態を確認し、少し待って再作成 |
| ログインが必要 | 投稿画面モードで「Xを開く」からログイン |
| ノートが保存されない | 「コミュニティノートを含める」をオン。取得できない場合は「Xを開く」で実際の表示とログインを確認 |
| 画像が欠ける | 読込警告を確認し、再作成。表示できない画像は補完しません |
| 投稿が長すぎる | 解像度を2倍または1倍へ変更 |
| 保存できない | 書込み可能な保存先と空き容量を確認 |
| コピーできない | PNGを保存し、そのファイルを利用 |
| 更新したい | アプリを終了し、新しい配布ZIPを別フォルダーに展開。設定・ログイン情報は引き継ぎます |
| 削除したい | アプリを終了して展開したフォルダーを削除。設定も消す場合は上記アプリデータフォルダーを削除 |

Xの仕様変更、削除、非公開設定、年齢・地域制限、アクセス制限によって取得できない場合があります。取得制限を回避する機能はありません。

## ライセンス

PostClipのソースコードはMIT Licenseです。ファイルは配布物ごとに次のように分けています。

| 配布物 | ファイル | 内容 |
| --- | --- | --- |
| ソース | `postclip/LICENSE` | PostClip自作部分のMIT License |
| Windows配布版 | `PostClip/LICENSE-PostClip.txt` | 同じPostClipのMIT License |
| Windows配布版 | `PostClip/LICENSE`、`PostClip/LICENSES.chromium.html` | Electron / Chromiumの著作権・ライセンス表記 |

ソースは `postclip-source-v1.1.2.zip` の `postclip/` に収めています。Windows配布版の `LICENSE` はElectron由来のファイルとして保持しています。

Xの投稿・画像・ロゴなどの権利は各権利者に帰属します。作成画像の公開・転載は、権利者の権利とXの利用条件に従って行ってください。PostClipはX非公式ツールです。

## 1.1.2の変更

- コミュニティノートを含める設定を標準でオンにし、旧設定も初回だけオンへ移行します。
- ノートがない通常の投稿も、設定を変えずに保存できるようにしました。
- 返信先に引用投稿があると余白が失われ、罫線がずれたり引用の右端が切れたりする問題を修正しました。

## 1.1.1の変更

- コミュニティノート付きで保存する設定を追加し、本文を確認できない場合の黙った省略を防ぎました。
- 投稿画面の新しい構造、写真やノートの遅延表示、記事と同じ表示枠にあるノートに対応しました。

## 1.1.0の変更

- 一番上の親から指定した投稿までを1枚に保存する機能を追加しました。
- 含める投稿を3段階で選べるようになりました。1.0.0の設定は従来と同じ意味で引き継ぎます。
